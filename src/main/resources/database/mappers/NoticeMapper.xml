<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  
 <mapper namespace="com.jerry.app.boards.notice.NoticeDAO">
 	<select id="getList" parameterType="pageDTO" resultType="NoticeDTO">
 		SELECT * FROM
		(
			SELECT ROWNUM R, N.* FROM
			(
				SELECT * FROM NOTICE_INFO
				WHERE BOARD_NUM > 0
				<choose>
					<when test="kind == 'k1'">
						 AND BOARD_TITLE LIKE '%' || #{search} || '%' 
					</when>
					<when test="kind == 'k2'">
						 AND BOARD_CONTENTS LIKE '%' || #{search} || '%' 
					</when>
					<when test="kind == 'k3'">
						 AND BOARD_WRITER LIKE '%' || #{search} || '%' 
					</when>
					<otherwise>
					</otherwise>
				</choose>
				ORDER BY BOARD_NUM DESC) N
		)
		WHERE R BETWEEN #{startRow} AND #{lastRow}
		
	</select>
	
	<select id="getTotalRowCount" parameterType="pageDTO" resultType="java.lang.Long">
		SELECT COUNT(BOARD_NUM)
		FROM NOTICE_INFO
		WHERE BOARD_NUM > 0
				<choose>
					<when test="kind == 'k1'">
						 AND BOARD_TITLE LIKE '%' || #{search} || '%' 
					</when>
					<when test="kind == 'k2'">
						 AND BOARD_CONTENTS LIKE '%' || #{search} || '%' 
					</when>
					<when test="kind == 'k3'">
						 AND BOARD_WRITER LIKE '%' || #{search} || '%' 
					</when>
					<otherwise>
					</otherwise>
				</choose>
	</select>
 	
 	<select id="getDetail" parameterType="NoticeDTO" resultType="NoticeDTO">
 		SELECT * FROM NOTICE_INFO
 		WHERE BOARD_NUM = #{board_num}
 	</select>
 	
 	<update id="hitUpdate" parameterType="NoticeDTO">
 		UPDATE NOTICE_INFO
 		SET	BOARD_HIT = #{board_hit}
		WHERE BOARD_NUM = #{board_num}
 	</update>
 	
 	<update id="doUpdate" parameterType="NoticeDTO">
 		UPDATE NOTICE_INFO 
		SET UPDATE_DATE = SYSDATE , BOARD_CATEGORY = #{board_category}, 
			BOARD_TITLE = #{board_title}, BOARD_CONTENTS = #{board_contents}
		WHERE BOARD_NUM = #{board_num}
 	</update>
 	
 	<delete id="doDelete" parameterType="NoticeDTO">
		DELETE FROM NOTICE_INFO
		WHERE BOARD_NUM = #{board_num}
 	</delete>
 	
 	<insert id="doAdd" parameterType="NoticeDTO">
		INSERT INTO NOTICE_INFO
		(BOARD_NUM, BOARD_WRITER, BOARD_TITLE, CREATE_DATE, BOARD_CONTENTS, BOARD_CATEGORY)
		VALUES
		(BOARD_SEQ.NEXTVAL, #{board_writer}, #{board_title}, SYSDATE, #{board_contents}, #{board_category})
 	</insert>
 
 </mapper>